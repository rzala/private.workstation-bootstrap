---
- name: Fedora Workstation Development Environment Setup
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Run host-level system setup
      import_playbook: host-setup.yml
      tags:
        - host
        - system
        - packages

    - name: Run user-level DevOps tools setup
      import_playbook: system.yml
      tags:
        - user
        - tools
        - devops
